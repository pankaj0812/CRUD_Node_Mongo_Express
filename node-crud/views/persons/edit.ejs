<!DOCTYPE html>
<html>
  <head>
    <title>Edit Detail</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
      <h3><a href="/persons">Persons List</a></h3>
      <h1>Edit Detail</h1>
      <form action="/persons/update/<%= person._id%>" method="post">
        <table>
          <tbody>
            <tr>
              <td>Name</td>
              <td><input type="text" name="name" value="<%= person.name %>" /></td>
            </tr>
            <tr>
              <td>Age</td>
              <td><input type="number" name="age" value="<%= person.age %>" /></td>
            </tr>
            <tr>
              <td>Gender</td>
              <td><input type="text" name="gender" value="<%= person.gender %>" /></td>
            </tr>
            <tr>
              <td>Mobile No</td>
              <td><input type="number" name="mobileno" value="<%= person.mobileno %>" /></td>
            </tr>
            <tr>
              <td colspan="2"><button type="submit">Update</button></td>
            </tr>
          </tbody>
        </table>
      </form>
    </div>
  </body>
</html>
